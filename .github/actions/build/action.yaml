name: Build
description: "just a test action"
runs:
  using: "composite"
  steps:
    - name: Test all
      shell: bash
      run: go test -v ./...
    - name: Build acnilbot
      shell: bash
      run: go build -v cmd/acnilbot/main.go