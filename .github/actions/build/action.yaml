name: Build
description: "just a test action"
runs:
  using: "composite"
  steps:
    - name: Test all
      shell: bash
      run: go test -v ./...
    - name: Build 
      shell: bash
      run: | 
        chmod +x ./tf/build.sh 
        ./tf/build.sh